{
  "name": "properview",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "dev": "pnpm -r --parallel dev",
    "format": "pnpm -r format",
    "format:fix": "pnpm -r format:fix",
    "prepare": "husky",
    "precommit": "lint-staged",
    "setup": "pnpm install && pnpm run prepare && pnpm run -r setup"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.1",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.5"
  }
}
